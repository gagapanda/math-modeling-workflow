# Migration Review Baseline

Private release baselines are not redistributed. Generate a new hash-bound package for the selected user case using the bundled migration tools. Passive analysis neither executes the model nor authorizes replacing its manifest. Human decisions and current source hashes remain required.
