> 当前状态（C4）：项目已采用 MIT；下文 C/C1/C2 的“许可待定”是历史阶段状态，不是当前 LICENSE 状态。尚未上传 GitHub。

# 阶段 C：公开候选迁移范围

状态：候选暂存，不是发布批准，也不是完整比赛环境。

## 已迁移的范围

通用能力只保留一份权威副本，位于 `.agents/skills/math-modeling/`：

- Skill 入口与调用元数据；
- 数据审计、模型路由、论文表达、验证与收口参考文档；
- 通用脚本及其 Schema；
- 四个通用模板：案例分析入口、Markdown 构建适配器、当前状态卡、论文收口审查记录；
- 对应测试源文件。

根目录 `scripts/`、`templates/`、`tests/` 不重复复制 Skill 内容，避免维护两套版本。

## 尚未提供的能力

| 内容 | 当前边界 |
|---|---|
| Word/PDF 实际导出工具 | 新写的可选 Pandoc/LibreOffice 后端已接入并实测；旧源码不迁移。完整 practice/submission 的所有下游步骤尚未统一切换，详见 paper-export.md |
| 下载的 LaTeX 类、字体及派生模板 | 整组暂缓，未复制字体、PDF、编译缓存或类文件 |
| 私人案例、数据与论文 | 未迁移。测试中自造的最小数据不代表真实比赛案例 |
| 历史审计、环境证明、具名复核 | 不继承；相关参考页替换为通用使用边界 |
| 其他专业 Skills | 未迁移；仅在用户环境确实提供时调用，否则说明不可用 |
| 官方规则 | 历史规则摘要不等于当届实时核验，实际比赛前须重新核验 |
| 发布许可与第三方来源确认 | C4 已按用户开放意愿实施 MIT；第三方边界见 notices，实际上传前仍需集合/隐私预检 |

## 本地检查入口

从仓库根目录运行，使用你已经配置的 Python 环境：

```text
python -B .agents/skills/math-modeling/scripts/check_skill.py --workspace-root . --skip-historical --json
```

完整检查保留原有测试，不把私人依赖缺失包装成通过。详细结果见整理者的阶段报告。此次检查使用既有环境，不代表已验证干净机器安装。

最小骨架命令（只创建空任务，不执行模型，也不产生可提交论文）：

```text
python -B .agents/skills/math-modeling/scripts/scaffold_case.py toy-demo --root ./local-work --profile explore
```

默认实践/提交骨架并非完整可运行示例。不要用探究模式冒充已完成论文与交付链。

## 迁移审查层级

本轮执行逐文件类型、内容模式、语法、引用和依赖筛查，并定向阅读风险文件；不是对每一行的独立人工版权确认，也不是对全部模型的语义正确性证明。没有凭据模式命中不意味着不存在任何隐私或秘密。

## 可选集成测试政策

首轮移植测试因未分发的组件出现 30 项缺文件错误。公开版仅对这些对应的测试添加存在性闸门：LaTeX 整个资产目录、Word 导出工具目录、私人协议文件未提供时，显示 `skipped` 和 `NOT VERIFIED`；它们一旦存在，原检查恢复执行，组件部分损坏不会自动放行。核心模型、证据绑定、收口和提交检查没有关闭。

跳过不等于通过，也不代表纸面输出功能可用。现有完整门即使返回 passed，也必须同时披露测试 skip 数和未提供功能；不得据此宣称完整比赛链通过。


## 阶段 C 历史实测结果（不含后续 C1）

- Schema：46/46 通过；Python AST：102/102 通过。
- 单元测试：发现 406 项，373 项通过，33 项跳过，0 失败、0 错误。
- 跳过范围：31 项依赖未分发组件的集成测试；2 项 Windows 符号链接权限受限测试。均未验证，不计为通过。
- 探究模式最小骨架创建和被动 doctor 检查通过；未执行正式模型或论文导出。
- 以上使用既有开发环境，未验证干净机器安装、完整 Word/PDF 生产链、真实比赛提交或版权授权。
- 原始 Skill 未改动。仍不可直接公开发布。

## C1 导出扩展

新增后端、JSON 配置、合成例子及专用回归，保留旧组件 skip 的明确边界。三页合成样例用于原生公式和实际页面验证；新的结构/哈希审查不是人工提交授权。具体边界与执行入口见 [paper-export.md](paper-export.md)。本轮完整门共 426 项测试：393 通过、33 跳过，无失败/错误；新增 20 项专项回归均通过。Schema 46 项通过；Skill 内 AST 103 项，全候选 AST 106 项。跳过仍是 31 项未分发旧组件集成与 2 项 Windows 符号链接权限测试，不计为通过。


## C2 新环境与许可证清单

已增加隔离环境最小入口、5 个导出包版本及 Windows/Python 3.12 wheel 锁、只读 doctor、逐文件来源完整性/授权闸门及 12 项独立回归。官方 Pandoc ZIP 重新下载并匹配 release digest；同机的新 venv、新路径、精简 PATH 实际导出与构建适配器通过，全部三页已实际 AI 查看。20 项导出专项和 12 项新回归均在隔离环境通过。

本轮文档不把新 Python 环境等同全新电脑。版权/项目许可仍待所有者核实，根 LICENSE 未改为开源许可；旧导出源码不迁移。详细证据范围、命令及未验证项见 [new-environment-validation.md](new-environment-validation.md)；清单见 [release-sources.md](release-sources.md)。

C2 稳定代码完整 Skill 回归已结束：426 项中 393 通过、33 跳过、0 失败/错误（289.019 秒）；Schema 46/46，Skill AST 103/103。此完整门使用既有建模测试环境，文档子进程显式使用新隔离解释器；不是所有建模库在新环境中验证。33 项跳过仍为 31 项未分发旧组件与 2 项 Windows 符号链接权限检查。另有 12 项根目录新回归在隔离环境通过，不在 426 项计数内。
